import 'package:flutter/material.dart';

class FlexibleNavigationRail extends StatelessWidget {
  const FlexibleNavigationRail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
