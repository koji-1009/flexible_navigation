import 'package:flutter/material.dart';

class FlexibleDrawer extends StatelessWidget {
  const FlexibleDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
