import 'package:flutter/material.dart';

class FlexibleNavigationBar extends StatelessWidget {
  const FlexibleNavigationBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
