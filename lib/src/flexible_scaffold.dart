import 'package:flutter/material.dart';

class FlexibleScaffold extends StatelessWidget {
  const FlexibleScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
